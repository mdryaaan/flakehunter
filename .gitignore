# Binaries
/bin/
/flakehunter
*.exe

# Test and coverage output
*.test
*.out
coverage.*

# Local scratch
*.local.json
# These names must match what the Makefile's `pipeline` target actually writes;
# they previously did not, so the artifacts were committed by accident.
scan.json
classified.json
eval.json
report.md
issue.md
digest.md

# Editor / OS
.DS_Store
.idea/
.vscode/
.claude/
